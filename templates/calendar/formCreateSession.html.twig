
    {{form_start(form)}}

    {{form_widget(form.free)}}
    {{form_widget(form.dateStart)}}
    {{form_widget(form.dateEnd)}}


    {{ form_row(form._token)}}
    {{form_end(form)}}
    
    <button id="add-session-customer" type="submit" class="btn btn-block button-session-add" data-action="save-session" data-id="{{customer.id}}">Save</button>

